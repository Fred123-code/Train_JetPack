<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.test.train_jetpack">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Train_JetPack"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning,UnusedAttribute"
        tools:replace="android:name" >
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 在清单文件中 定义 后台播放的服务 -->
        <service android:name=".player.notification.PlayerService" />

        <!-- 在清单文件中 定义 广播接受者 -->
        <receiver
            android:name=".player.notification.PlayerReceiver"
            tools:ignore="ExportedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="pure_music.xiangxue_derry.close" /> <!-- 关闭的Action -->
                <action android:name="pure_music.xiangxue_derry.pause" /> <!-- 暂停的Action -->
                <action android:name="pure_music.xiangxue_derry.next" /> <!-- 下一首的Action -->
                <action android:name="pure_music.xiangxue_derry.play" /> <!-- 播放的Action -->
                <action android:name="pure_music.xiangxue_derry.previous" /> <!-- 上一首的Action -->
                <action android:name="android.intent.action.MEDIA_BUTTON" />
                <action android:name="android.media.AUDIO_BECOMING_NOISY" />
            </intent-filter>
        </receiver>
    </application>

</manifest>